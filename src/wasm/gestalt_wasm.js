/**
 * Stub file for gestalt-wasm WASM module.
 * The real WASM will be compiled with wasm-pack and placed here.
 * This stub allows Vite to resolve the dynamic import at build time
 * without failing. The runtime dynamic import will still fail (expected)
 * and the worker gracefully falls back to MockGestaltEngine.
 */
throw new Error('gestalt-wasm not yet compiled — use MockGestaltEngine fallback');
